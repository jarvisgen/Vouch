// Agent service entrypoint: routes a task to its worker, runs the auditor, stores the
// evidence bundle on Walrus, and (for the demo) drives the resolve PTB.
//
// TODO(step 4): wire workers + auditor + walrus + txns into the end-to-end loop.
console.log("vouch agents — scaffold. See TODO(step 4) markers.");
