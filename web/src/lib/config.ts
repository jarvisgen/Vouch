// Reads deployments/testnet.json + network config for the dapp.
// TODO(step 5): import deployment ids; expose networkConfig for SuiClientProvider.
export const NETWORK = "testnet" as const;
export const RPC = "https://fullnode.testnet.sui.io:443";
