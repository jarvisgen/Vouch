// Reads a task-class DeepBook pool: YES mid-price (= reliability) + order book levels.
// Polls so the book visibly reprices after resolution.
// TODO(step 5): DeepBook SDK queries; expose { reliabilityBps, bids, asks }.
export {};
