// Result page: worker output → auditor verdict → Walrus evidence link → resolution
// (payout/slash). Drives the FAILURE-branch demo.
// TODO(step 5)
export default function Result() {
  return null;
}
