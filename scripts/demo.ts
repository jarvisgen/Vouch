// Scripted end-to-end FAILURE-branch run for the demo:
//   hire ClauseBot w/ guarantee (PTB #1) → worker returns flawed analysis →
//   auditor catches it → evidence to Walrus → resolve(FAIL) (PTB #2):
//   user paid out, bond slashed, reliability drops, order book reprices.
//
// TODO(step 6): orchestrate agents/ + scripts/ to produce the full on-stage loop.
console.log("TODO(step 6): scripted failure-branch demo run");
