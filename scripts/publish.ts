// Publishes the Move package to testnet and writes deployments/testnet.json
// (packageId, agentRegistryId, reservePoolId, per-task-class yes coin types).
//
// TODO(step 3/6): `sui move build` + publish via SDK or CLI; parse created objects;
// write deployments/testnet.json from the template shape.
console.log("TODO(step 6): publish Move package -> deployments/testnet.json");
